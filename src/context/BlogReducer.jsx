export const blogReducer = (state,action)=>{
 switch(action.type){
    case "Blog_CREATE" :
        return state;
    case "BLOG_DELETE" :
        return state;
        default:
        return state;
 }
}
